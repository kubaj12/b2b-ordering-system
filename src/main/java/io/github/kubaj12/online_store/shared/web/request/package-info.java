/**
 * HTML request and response primitives shared by the module-owned Spring MVC web adapters.
 *
 * <p>HTMX headers select only between full-page and fragment HTML representations. Security,
 * validation, and application-service execution remain independent of this package.</p>
 */
package io.github.kubaj12.online_store.shared.web.request;
