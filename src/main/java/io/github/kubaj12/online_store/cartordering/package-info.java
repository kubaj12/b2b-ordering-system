/**
 * Cart and ordering module.
 *
 * <p>Owns persistent carts, checkout reviews and tokens, submitted orders, immutable order
 * snapshots, and order intake status. It coordinates other modules only through their application
 * boundaries and exposes its own application boundary for collaboration.</p>
 */
package io.github.kubaj12.online_store.cartordering;
