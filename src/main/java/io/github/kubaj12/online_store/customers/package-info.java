/**
 * Customers module.
 *
 * <p>Owns customer company profiles and billing data. It uses identity references through the
 * identity and access application boundary and exposes customer information only through its own
 * application boundary.</p>
 */
package io.github.kubaj12.online_store.customers;
