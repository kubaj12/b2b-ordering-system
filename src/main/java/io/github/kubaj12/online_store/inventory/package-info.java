/**
 * Inventory module.
 *
 * <p>Owns the single mutable available quantity for each catalog SKU and the history of inventory
 * changes. Other modules may collaborate with it only through its application boundary.</p>
 */
package io.github.kubaj12.online_store.inventory;
